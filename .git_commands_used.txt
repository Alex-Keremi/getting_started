git --version
git config --global user.name "Alex Keremi"
git config --global user.email "getalexkeremi@gmail.com"
git config --list  [see all configurations]


git <verb,eg, config> --help  or/  git help <verb>		in need of help? 


git init
ls -la		list all files explicitly
rm -rf .git		to stop tracking project with git
touch .gitignore		txt files to add files u want git to ignore tracking


git status		untracked & modified will be in working dir, staging area where we organize what we want to be committed
git add <single file>	add single file to staging area
	git add -A	add all files to staging area

git reset <single file>	  or/ 	git --cached <file>	to remove a file from staging area
git reset	to remove all files from staging area


git commit -m "<msg of changes made>"

git log		see commits made


git clone <url> <where to clone>
  
